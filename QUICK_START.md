# SearchMenu 快速开始指南（7-9 天计划）

## 📅 时间表总结

| 天数 | 任务 | 预计耗时 | 优先级 |
|------|------|--------|--------|
| **第1天** | 环境配置 + 数据库初始化 | 4-8h | 🔴 必须 |
| **第2天** | 后端核心 API 实现 | 4-8h | 🔴 必须 |
| **第3天** | 完善后端 API | 4-8h | 🔴 必须 |
| **第4天** | 前端基础 + API 集成 | 4-8h | 🔴 必须 |
| **第5天** | 搜索 + 菜品详情 | 4-8h | 🟡 重要 |
| **第6天** | 调试 + 优化 | 4-8h | 🟡 重要 |
| **第7-9天** | 部署 + 扩展 | 4-8h | 🟢 可选 |

**总工作量**：28-72 小时 ✅

---

## 🚀 立即开始（第一步）

### 1️⃣ 创建项目目录
```bash
mkdir SearchMenu && cd SearchMenu
git init
mkdir backend frontend
```

### 2️⃣ 初始化后端
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

### 3️⃣ 安装依赖
```bash
cat > requirements.txt << 'EOF'
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
jieba==0.42.1
python-dotenv==1.0.0
requests==2.31.0
EOF

pip install -r requirements.txt
```

### 4️⃣ 查看详细步骤
👉 **请阅读 outline.md 中的"立即执行步骤"部分**

---

## 📁 项目最终结构

```
SearchMenu/
├── backend/
│   ├── venv/                  # 虚拟环境
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py            # FastAPI 主应用
│   │   ├── database.py        # 数据库连接
│   │   ├── models.py          # SQLAlchemy 模型
│   │   └── services/          # 业务逻辑
│   ├── scripts/
│   │   └── init_db.py         # 数据库初始化
│   ├── search_menu.db         # SQLite 数据库
│   └── requirements.txt
│
├── frontend/
│   ├── src/
│   │   ├── main.js            # 主逻辑
│   │   ├── api.js             # API 客户端
│   │   └── styles.css         # 样式
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
├── outline.md                 # 项目文档（本文件）
└── README.md                  # 项目说明
```

---

## 🔑 核心要点

### ✅ 第1天的目标
- 数据库创建成功
- 有至少 3-5 个示例菜品
- 能够通过 SQLite 查询验证

### ✅ 第2-3天的目标
- 5 个 API 端点全部实现：
  - `GET /dishes/random` - 随机推荐
  - `POST /dishes/recommend` - 基于食材推荐
  - `GET /dishes/search` - 搜索
  - `GET /dishes/{id}` - 菜品详情
  - `GET /ingredients` - 食材列表

### ✅ 第4-5天的目标
- 前端可以连接后端 API
- 所有核心功能可用（随机、推荐、搜索、详情）
- 没有明显的 bug

### ✅ 第6天的目标
- 应用稳定运行
- 用户体验可接受
- 准备好演示或部署

---

## 💡 关键技术点

### 后端
- **FastAPI**：简洁高效的 Web 框架
- **SQLAlchemy**：ORM 框架管理数据库
- **SQLite**：轻量级数据库，无需额外配置
- **CORS 中间件**：允许前端跨域访问

### 前端
- **Vanilla JavaScript**：不用框架，快速开发
- **Tailwind CSS**：快速编写 CSS，无需写样式
- **Fetch API**：调用后端 API

### 数据库
- **Dish 表**：菜品信息
- **Ingredient 表**：食材信息
- **DishIngredient 表**：关联表（菜品 ← → 食材）

---

## 🆘 常见问题

### Q: 数据爬取太复杂，怎么办？
A: 使用示例数据快速开始！在 `init_db.py` 中已提供 3 个示例菜品，足够演示。

### Q: 前端开发太慢？
A: 我们使用 Vanilla JS + Tailwind，已经是最快的方案了。不涉及编译，实时看到效果。

### Q: API 匹配算法不准？
A: 先实现简单的 `in` 操作符匹配，后续再优化。本方案已足够展示功能。

### Q: 如果我时间紧张？
A: 优先完成第 1-4 天，第 5-6 天可以跳过，直接用 localhost 演示。

---

## 📞 需要帮助？

- **卡在某一步？** 查看 outline.md 的"立即执行步骤"部分
- **代码错误？** 告诉我错误信息，我会帮调试
- **时间不够？** 可以跳过第 7-9 天的部署

---

## ✨ 完成检查清单

```
第1天 (环境配置)：
- [ ] 创建项目目录
- [ ] 建立虚拟环境
- [ ] 创建数据库模型
- [ ] 导入示例数据
- [ ] 验证数据库

第2天 (后端API #1)：
- [ ] 创建 FastAPI 应用
- [ ] 实现 /random API
- [ ] 实现 /recommend API
- [ ] 测试两个 API

第3天 (后端API #2-5)：
- [ ] 实现 /search API
- [ ] 实现 /dishes/{id} API
- [ ] 实现 /ingredients API
- [ ] 测试所有 API

第4天 (前端基础)：
- [ ] 初始化前端项目
- [ ] 创建 HTML 页面
- [ ] 创建 API 客户端
- [ ] 连接前后端
- [ ] 测试完整流程

第5天 (搜索+详情)：
- [ ] 实现搜索功能
- [ ] 实现菜品详情模态框
- [ ] 美化 UI
- [ ] 测试所有交互

第6天 (调试+优化)：
- [ ] 完整 E2E 测试
- [ ] 修复所有 bug
- [ ] 性能优化
- [ ] 准备演示

第7-9天 (部署-可选)：
- [ ] 部署后端
- [ ] 部署前端
- [ ] 编写 README
- [ ] 制作演示视频
```

---

## 📚 引用资源

- **FastAPI 文档**：https://fastapi.tiangolo.com/
- **Tailwind CSS**：https://tailwindcss.com/
- **SQLAlchemy**：https://www.sqlalchemy.org/
- **Vite**：https://vitejs.dev/

---

**祝你开发顺利！🚀 如有问题随时反馈。**
